<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            android:id="@+id/layout_toolbar"
            layout="@layout/layout_toolbar" />

        <LinearLayout
            android:id="@+id/content_frame"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/layout_toolbar"
            android:orientation="vertical">

            <Button
                android:id="@+id/button_hello"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="hello.txt"
                android:textColor="@color/secondaryTextColor"
                android:backgroundTint="@color/oppositeColor" />

            <Button
                android:id="@+id/button_reddit"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="reddit.png"
                android:textColor="@color/secondaryTextColor"
                android:backgroundTint="@color/oppositeColor" />

            <Button
                android:id="@+id/button_trex"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="t-rex.jpg"
                android:textColor="@color/secondaryTextColor"
                android:backgroundTint="@color/oppositeColor" />

            <Button
                android:id="@+id/button_rocket"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="rocket.pdf"
                android:textColor="@color/secondaryTextColor"
                android:backgroundTint="@color/oppositeColor" />

        </LinearLayout>

    </RelativeLayout>

</android.support.design.widget.CoordinatorLayout>
